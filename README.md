# MemePlatform 🎨

Полнофункциональная платформа для мемов с веб-интерфейсом и Telegram-ботом.

## Возможности

### Веб-интерфейс
- 🖼 Галерея мемов с категориями
- 🔍 Поиск и фильтрация
- 👤 Регистрация и авторизация
- ⬆️ Загрузка мемов (до 50 МБ)
- ❤️ Лайки и шаринг
- 💬 Комментарии

### Админ-панель (веб)
- 📊 Дашборд со статистикой
- ✅ Модерация мемов (очередь)
- 📁 Управление категориями
- 👥 Управление пользователями
- 🔄 Массовые операции

### Telegram-бот
- 🎨 Генератор мемов с шаблонами
- 📤 Загрузка через бота
- 🔗 Интеграция с веб-платформой

## Установка

```bash
# Виртуальное окружение
python -m venv .venv
source .venv/bin/activate  # Linux/Mac
# или .venv\Scripts\activate  # Windows

# Зависимости
pip install -r requirements.txt
```

## Настройка

Создайте файл `.env`:

```env
BOT_TOKEN=your_bot_token_here
ADMIN_IDS=123456789,987654321
SECRET_KEY=your_secret_key_for_sessions
```

## Запуск

### Веб-интерфейс

```bash
uvicorn web_app:app --host 0.0.0.0 --port 8000 --reload
```

Откройте http://localhost:8000

### Telegram-бот

```bash
python main.py
```

### Оба одновременно

```bash
# Терминал 1: uvicorn web_app:app --host 0.0.0.0 --port 8000
# Терминал 2: python main.py
```

## API

### Публичные
- `GET /api/memes` — Список мемов
- `GET /api/categories` — Категории

### Авторизованные  
- `POST /api/meme/{id}/like` — Лайк
- `POST /api/meme/{id}/share` — Шаринг

### Админские
- `POST /api/meme/{id}/approve` — Одобрить
- `POST /api/meme/{id}/reject` — Отклонить
- `DELETE /api/meme/{id}` — Удалить
- `POST /api/bulk/approve` — Массовое одобрение
- `POST /api/bulk/reject` — Массовое отклонение

## Технологии

- **Backend**: FastAPI, aiogram 3
- **Database**: SQLite (aiosqlite)
- **Frontend**: Bootstrap 5, Jinja2

## Лицензия

MIT

