[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uvicorn web_app:app --host 0.0.0.0 --port ${PORT:-8000}"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[variables]
PYTHONUNBUFFERED = "1"
